MONGO_URI=mongodb://localhost:27017/exhibition
JWT_SECRET=change-this-in-local-dev
DUMMY_VISITOR_EMAIL=user@gmail.com
DUMMY_VISITOR_PASSWORD=user123
DUMMY_EXHIBITOR_EMAIL=exhibitor@gmail.com
DUMMY_EXHIBITOR_PASSWORD=exhibitor123

# The frontend auto-detects this backend's LAN address from the Expo dev server,
# so no manual IP configuration is needed. To override, set EXPO_PUBLIC_API_URL
# in frontend/.env instead (see frontend/.env.example).