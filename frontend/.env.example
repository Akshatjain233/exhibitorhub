# Leave this unset in normal dev — the app auto-detects the backend's address
# from the Expo dev server's host, so it works over LAN without any manual IP.
# Only set this to override auto-detection (e.g. pointing at a deployed backend).
# EXPO_PUBLIC_API_URL=http://localhost:5000/api/v1
